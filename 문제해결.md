# 문제해결

README를 따라 하다가 막히면 이 문서를 확인하세요.

## Today chunks가 0입니다

브라우저 확장에서 입력이 잡히지 않은 상태입니다.

확인할 것:

1. 테스트 중인 페이지를 새로고침했나요?
2. 확장 프로그램을 새로 빌드한 뒤 `chrome://extensions`에서 새로고침했나요?
3. 허용된 도메인에서 입력했나요?
4. 최소 8자 이상 입력했나요?
5. 입력 후 2초 정도 기다렸나요?

개발자도구 콘솔에 아래 로그가 나오면 캡처 준비가 된 것입니다.

```text
Browser Worklog capture enabled.
```

입력이 잡히면 아래 로그가 나옵니다.

```text
Browser Worklog captured chunk draft.
```

## No accessible data sources found

Notion 토큰은 맞지만, 토큰이 접근할 수 있는 데이터베이스가 없다는 뜻입니다.

해결:

1. Notion 왼쪽 사이드바에서 `설정`을 엽니다.
2. `연결`을 엽니다.
3. `개발자 포털`을 엽니다.
4. 이 프로젝트용 토큰을 엽니다.
5. `콘텐츠 사용 권한` 또는 `Content access`를 엽니다.
6. 작업일지 데이터베이스를 추가합니다.
7. Options 페이지에서 `접근 가능한 DB 찾기`를 다시 누릅니다.

## Could not find database 또는 object_not_found

거의 항상 권한 문제입니다.

확인할 것:

1. DB ID가 맞는지 확인합니다.
2. 토큰의 워크스페이스가 데이터베이스가 있는 워크스페이스와 같은지 확인합니다.
3. `콘텐츠 사용 권한`에 데이터베이스가 들어 있는지 확인합니다.

## Cannot create new title property

Notion DB에는 title 속성이 하나만 있을 수 있습니다.

이 프로젝트는 기존 title 속성이 `이름`이어도 자동으로 사용하도록 되어 있습니다. 이 에러가 계속 나오면 확장 프로그램을 다시 빌드하고, `chrome://extensions`에서 새로고침하세요.

```powershell
cd "browser-worklog-to-notion"
npm run build
```

## Schema failed가 나옵니다

Options 페이지의 상태 메시지를 그대로 읽어보세요.

대부분은 다음 중 하나입니다.

```text
토큰이 틀림
데이터베이스 권한이 없음
데이터베이스 ID가 틀림
같은 이름의 속성이 있는데 타입이 다름
```

## PC용 dry-run은 되는데 run이 실패합니다

먼저 `.env`를 확인합니다.

확인할 것:

```text
.env에 NOTION_TOKEN=ntn_... 값이 있는지
.env에 NOTION_DATABASE_ID=... 값이 있는지
Notion 콘텐츠 사용 권한에 DB가 들어 있는지
```

## 처음 실행했는데 수집 내용이 적습니다

PC용 도구는 기본적으로 첫 실행에서 기준점을 만드는 방식입니다.

처음 실행은 “이제부터 볼 준비”에 가깝고, 이후 실행부터 이전 성공 시점 이후의 변경을 모읍니다.

## 보안 주의

절대 커밋하면 안 되는 것:

```text
.env
NOTION_TOKEN
ntn_... 토큰
개인 Notion 데이터베이스 ID
업무 원문 데이터
```

## 참고한 공식 문서

- [Internal connections - Notion Developers](https://developers.notion.com/guides/get-started/internal-connections)
- [Working with databases - Notion Developers](https://developers.notion.com/guides/data-apis/working-with-databases)
- [Search by title - Notion API Reference](https://developers.notion.com/reference/post-search)
