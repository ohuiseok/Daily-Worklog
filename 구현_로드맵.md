# 자동 작업일지 Notion 업로드 구현 로드맵

## 문서 목적

이 문서는 두 개의 오픈소스 프로젝트를 실제로 구현하기 위한 작업 순서와 고려사항을 정리한다.

대상 프로젝트:

1. `desktop-worklog-to-notion`
   - Python 기반
   - Windows Desktop 파일 변경, Git 기록, 문서/엑셀/CSV 구조를 분석해 Notion에 작업일지를 만든다.

2. `browser-worklog-to-notion`
   - Chrome/Edge Extension 기반
   - 브라우저에서 사용자가 작성한 텍스트를 로컬에 임시 저장하고, 경량 요약 후 Notion에 작업일지를 만든다.

두 프로젝트는 독립적으로 동작해야 한다. 사용자는 둘 중 하나만 설치할 수도 있고, 둘 다 같은 Notion Database에 연결할 수도 있다.

## 최종 제품 방향

### 공통 원칙

- Notion에는 파일 원문이나 브라우저 작성 원문을 업로드하지 않는다.
- Notion에는 날짜별 작업 요약, 통계, 근거 메타데이터만 업로드한다.
- `last_success_at` 이후부터 현재까지의 변경분만 처리한다.
- 업로드가 성공했을 때만 `last_success_at`을 갱신한다.
- 첫 실행은 안전하게 기준점을 만드는 방식으로 처리한다.
- 사용자의 개인 경로와 토큰은 Git에 올리지 않는다.
- 모든 설정은 예시 파일과 개인 설정 파일을 분리한다.

### 공통 Notion Database 권장 스키마

| 속성명 | 타입 | 설명 |
| --- | --- | --- |
| Name | Title | 작업일지 제목 |
| Date | Date | 작업 날짜 |
| Source | Select | `Desktop`, `Browser` |
| Project | Text | 프로젝트명 또는 작업 영역 |
| Status | Select | `Success`, `Partial`, `Failed` |
| Summary | Rich text | 짧은 요약 |
| Commit Count | Number | Desktop source에서 사용 |
| Modified Files | Number | Desktop source에서 사용 |
| Written Chunks | Number | Browser source에서 사용 |
| Main Domains | Multi-select | Browser source에서 사용 |

제외한 속성:

- `Keywords`: 자동 추출 품질이 낮고 사용자에게 의미 있는 정보가 적어 제거한다.

권장 Notion view:

- `Calendar`: `Date` 기준 캘린더
- `All Logs`: 전체 테이블
- `Desktop Logs`: `Source = Desktop` 필터
- `Browser Logs`: `Source = Browser` 필터
- `Failed Runs`: `Status != Success` 필터

## 전체 구현 순서

초기 권장 순서:

1. 공통 Notion Database 스키마 확정
2. `desktop-worklog-to-notion` MVP 구현
3. Desktop 프로젝트의 설정, 상태 저장, Notion upsert 안정화
4. Desktop 프로젝트 README와 설치 스크립트 작성
5. `browser-worklog-to-notion` MVP 구현
6. Browser 프로젝트의 Local Detail Mode, 경량 요약, Notion upsert 안정화
7. 두 프로젝트의 Notion 스키마 호환 검증
8. 샘플 데이터와 테스트 시나리오 정리
9. GitHub 공개용 문서 정리

이 순서를 추천하는 이유:

- Desktop 프로젝트가 기술적으로 더 안정적이고 먼저 완성하기 쉽다.
- Notion 스키마와 upsert 로직을 먼저 검증하면 Browser 프로젝트가 같은 규칙을 재사용할 수 있다.
- Browser 프로젝트는 개인정보와 브라우저 제약이 크므로 별도 MVP로 작게 시작하는 편이 낫다.

## 현재 진행 기준

문서 작성 시점 기준으로 실제 진행은 초기 권장 순서와 다르게 진행되었다.

현재 상태:

- `browser-worklog-to-notion`은 MVP 기능이 상당 부분 구현되어 있다.
- 브라우저 확장은 Notion `data_sources` API와 기존 `databases` API fallback을 고려한다.
- 브라우저 확장은 `Keywords`를 제거하고, 사용자가 작성한 내용 기반의 요약과 근거 중심으로 Notion 본문을 만든다.
- 브라우저 Options UI는 한국어 설정 흐름, 토큰 저장, 접근 가능한 DB 찾기, DB ID 저장, 스키마 준비 버튼을 제공한다.
- 루트 README는 입구 문서로 줄였고, 브라우저용/PC용 README를 분리했다.
- Desktop 개인 설정은 `.env` 하나로 통합했고, `.worklog`는 상태/스냅샷 저장만 담당한다.
- `desktop-worklog-to-notion`은 CLI, 설정, 스캔, 스냅샷, Git 수집, 요약, Notion 업로드 뼈대가 있다.
- Desktop 최종 배포는 Python 설치 없는 Windows 11 `.exe`를 목표로 한다.
- 비개발자용 자동 실행은 Windows 작업 스케줄러가 아니라 시작프로그램 바로가리를 사용한다.
- 다만 Desktop Notion client는 최신 Notion `data_sources` 방식과 브라우저 확장의 실제 운용 경험을 반영해 다시 맞춰야 한다.

앞으로의 우선순위:

1. Desktop Notion client를 최신 Notion 방식에 맞춘다.
2. Desktop 스키마를 Browser와 완전히 호환되게 맞춘다.
3. Desktop dry-run 결과가 실제 사용자 Desktop 기준으로 과하지 않은지 검증한다.
4. Desktop 실제 Notion 업로드를 검증한다.
5. Desktop 자동 실행과 첫 실행 기준점 동작을 확인한다.
6. Browser 남은 마감 작업이 있으면 보완한다.
7. 두 프로젝트를 같은 Notion DB에 붙여 통합 검증한다.

## 1단계: 공통 Notion 설계

### 목표

두 프로젝트가 같은 Notion Database를 사용할 수 있도록 최소 공통 스키마를 먼저 확정한다.

### 구현 내용

- Notion integration 생성 방법 문서화
- 사용자가 Notion Database를 만들고 integration에 권한을 주는 절차 작성
- Database ID 확인 방법 작성
- `Date + Source + Project` 조합으로 기존 페이지를 검색하는 upsert 규칙 설계
- 페이지 생성 payload 예시 작성
- 페이지 본문 block 생성 규칙 설계

### 고려사항

- Notion API 버전은 코드 상수로 관리한다.
- API 요청 실패 시 재시도 전략을 둔다.
- Notion block 개수 제한을 고려해 긴 목록은 접는다.
- Notion 토큰은 `.env` 또는 확장 프로그램 로컬 저장소에만 저장한다.
- Notion에는 원문을 올리지 않는다는 정책을 README에 명확히 쓴다.

### 완료 기준

- 테스트용 Database에 `Desktop` source 페이지를 만들 수 있다.
- 테스트용 Database에 `Browser` source 페이지를 만들 수 있다.
- 같은 `Date + Source + Project` 페이지가 있으면 새로 만들지 않고 업데이트할 수 있다.

## 2단계: desktop-worklog-to-notion MVP

### 목표

Windows Desktop 전체를 스캔해서 파일 변경/Git 기록을 수집하고 Notion에 날짜별 작업일지를 만든다.

### 권장 기술 스택

```text
Python 3.11+
requests
python-dotenv
openpyxl
```

선택:

```text
pandas는 MVP에서 제외
GitPython은 MVP에서 제외하고 subprocess로 git 사용
```

### 권장 저장소 구조

```text
desktop-worklog-to-notion
├── README.md
├── requirements.txt
├── .gitignore
├── .env.example
├── worklog
│   ├── __init__.py
│   ├── cli.py
│   ├── config.py
│   ├── paths.py
│   ├── scanner.py
│   ├── git_collector.py
│   ├── analyzers.py
│   ├── snapshot.py
│   ├── summarizer.py
│   ├── notion_client.py
│   ├── scheduler.py
│   └── logging_utils.py
├── .worklog
│   └── state/
└── tests
    ├── test_paths.py
    ├── test_snapshot.py
    ├── test_summarizer.py
    └── test_sensitive_filter.py
```

### 설정 파일

Git에 포함:

```text
.env.example
```

Git에서 제외:

```text
.worklog/state/
.worklog/logs/
.env
```

`.env.example` 예시:

```text
NOTION_TOKEN=ntn_xxx
NOTION_DATABASE_ID=your_database_or_data_source_id
NOTION_SOURCE=Desktop

WORKLOG_PROJECT_NAME=Desktop Worklog
WORKLOG_ROOT_PATHS=%USERPROFILE%/Desktop
WORKLOG_TIMEZONE=Asia/Seoul

WORKLOG_MAX_FILE_SIZE_KB=1024
WORKLOG_EXCLUDE_DIRS=.git;node_modules;.venv;venv;dist;build;.next;.cache;__pycache__;$RECYCLE.BIN
WORKLOG_EXCLUDE_EXTENSIONS=.lnk;.png;.jpg;.jpeg;.gif;.webp;.ico;.zip;.7z;.rar;.exe;.msi;.dll;.tmp;.log
WORKLOG_EXCLUDE_NAME_PATTERNS=~$*;스크린샷*;Screenshot*
```

정책:

- 개인 설정은 `.env` 하나에 모은다.
- `.worklog`는 상태, 스냅샷, 로그만 저장한다.
- 여러 경로는 `;`로 구분한다.
```

### 구현 순서

#### 2.1 CLI 뼈대

명령:

```text
worklog init
worklog run
worklog dry-run
worklog status
```

구현:

- `argparse`로 시작한다.
- `init`은 `.env`가 없으면 `.env.example`을 복사한다.
- `dry-run`은 Notion 업로드 없이 콘솔과 로컬 JSON만 생성한다.
- `run`은 실제 Notion 업로드를 수행한다.
- `install-scheduler`는 최종 UX에서 사용하지 않는다. 자동 실행은 exe 단계의 시작프로그램 바로가리로 처리한다.

완료 기준:

- `python -m worklog.cli dry-run`이 동작한다.
- `.env` 설정이 빠졌을 때 친절한 오류 또는 init 안내가 나온다.

#### 2.2 경로 처리

구현:

- `%USERPROFILE%`, `${USERPROFILE}`, `~` 확장
- `/`와 `\` 모두 허용
- 상대 경로는 프로젝트 루트 기준 처리
- 존재하지 않는 경로는 경고만 남기고 건너뛴다.

테스트:

- `%USERPROFILE%/Desktop`이 실제 사용자 Desktop으로 변환되는지 확인
- 존재하지 않는 경로 처리
- 한글 경로 처리

#### 2.3 상태 저장

파일:

```text
.worklog/state/run_state.json
.worklog/state/latest_snapshot.json
.worklog/state/YYYY-MM-DD.json
```

정책:

- `last_success_at`이 없으면 첫 실행으로 판단한다.
- 첫 실행 기본값은 `baseline_only`다.
- 모든 업로드가 성공한 뒤에만 `last_success_at`을 갱신한다.
- 실패하면 다음 실행에서 같은 범위를 다시 처리한다.

완료 기준:

- 첫 실행 시 기준 스냅샷만 생성된다.
- 두 번째 실행부터 변경분만 잡힌다.

#### 2.4 Desktop 파일 스캐너

수집:

- 상대 경로
- 확장자
- 파일 크기
- 마지막 수정 시간
- SHA-256 해시
- 파일 타입

제외:

- 설정의 제외 폴더
- 제외 확장자
- 제외 이름 패턴
- 숨김/시스템 파일은 가능하면 제외
- 최대 파일 크기 초과 파일

주의:

- Desktop 전체 스캔이므로 실패 파일 하나 때문에 전체 실행이 중단되면 안 된다.
- 권한 오류는 skipped 목록에 남긴다.
- 파일명에도 민감정보가 있을 수 있으므로 마스킹 옵션을 고려한다.

완료 기준:

- Desktop에서 오늘 또는 수집 범위 내 수정된 파일만 수집된다.
- 제외 규칙이 동작한다.
- 오류 파일이 있어도 전체 실행은 계속된다.

#### 2.5 파일 타입별 분석

텍스트:

- 라인 수
- 글자 수
- 제목 후보
- 문서 주제 후보
- 원문은 Notion에 업로드하지 않음

CSV:

- 컬럼 수
- 행 수
- 컬럼명
- 인코딩 `utf-8-sig`, 실패 시 `cp949`
- 행 데이터 원문은 업로드하지 않음

Excel:

- 시트명
- 시트별 행/열 수
- 헤더 후보
- 셀 데이터 원문은 업로드하지 않음

완료 기준:

- `.txt`, `.md`, `.csv`, `.xlsx` 샘플을 분석할 수 있다.
- 깨진 파일이나 암호화 파일은 오류 목록에 남고 실행은 계속된다.

#### 2.6 Git 수집

방법:

- Desktop 하위에서 Git repo를 찾는다.
- `.git` 폴더가 있는 루트를 repo로 인식한다.
- 중첩 repo는 중복 처리하지 않도록 주의한다.

명령:

```bash
git rev-parse --is-inside-work-tree
git branch --show-current
git log --since=<last_success_at> --pretty=format:"%h|%ad|%s" --date=iso
git status --short
git diff --stat
git diff --cached --stat
```

주의:

- diff 원문은 기본 수집하지 않는다.
- repo가 깨져 있거나 git 명령이 실패해도 전체 실행은 계속한다.
- commit 시간은 로컬 시간대로 변환해 날짜별 그룹화한다.

완료 기준:

- Git repo가 있으면 커밋, 브랜치, status, diff stat이 수집된다.
- Git repo가 없어도 정상 종료된다.

#### 2.7 스냅샷 비교

비교:

- 새 파일
- 수정 파일
- 삭제 파일
- 확장자별 변경량
- 폴더별 변경량

제외:

- 이름 변경 감지는 MVP에서 제외
- 셀 단위 Excel diff는 MVP에서 제외

완료 기준:

- 이전 스냅샷과 현재 스냅샷을 비교해 변경 파일이 분류된다.

#### 2.8 규칙 기반 요약

입력:

- 파일 타입별 변경량
- 많이 변경된 폴더
- Git 커밋 메시지
- 미커밋 파일 수
- Excel/CSV 구조 변화

출력 예시:

```text
- Git 커밋 2개를 기록했다.
- Python 파일 3개와 Markdown 문서 1개가 수정되었다.
- Desktop 하위 worklog 프로젝트에서 변경이 집중되었다.
- Excel 파일 1개에서 시트 구조 변경이 감지되었다.
- 커밋되지 않은 변경 파일 3개가 남아 있다.
```

완료 기준:

- 변경량이 없으면 빈 작업일지를 만들지 않거나 `No changes` 상태로 처리한다.
- Notion에 올릴 본문 block이 생성된다.

#### 2.9 Notion 업로드

구현:

- `Date + Source + Project`로 기존 페이지 검색
- 있으면 업데이트
- 없으면 생성
- 본문은 섹션별 block으로 구성

본문:

- 오늘 한 일
- Git
- 파일 변경 요약
- 문서/표 작업 요약
- 스냅샷 비교
- 실행 로그

실패 처리:

- 429 rate limit이면 대기 후 재시도
- 401/403이면 토큰/권한 오류로 명확히 표시
- 부분 실패 시 `Status = Partial`

완료 기준:

- dry-run JSON과 실제 Notion 페이지 내용이 일치한다.
- 중복 페이지가 생기지 않는다.

#### 2.10 Windows 11 exe 배포와 자동 실행

목표:

- Python이 없는 Windows 11 사용자도 실행할 수 있게 PyInstaller 기반 `.exe`로 배포한다.
- 사용자가 다운로드 폴더의 원본 exe를 삭제해도 자동 실행이 깨지지 않게 한다.

설치 위치:

```text
%LOCALAPPDATA%\DesktopWorklogToNotion\
├─ app\
│  └─ desktop-worklog-to-notion.exe
├─ logs\
│  └─ worklog.log
└─ state\
   └─ run_state.json

%APPDATA%\DesktopWorklogToNotion\
└─ settings.json
```

자동 실행:

- 시작프로그램 폴더에 바로가리를 만든다.
- 바로가리는 다운로드한 원본 exe가 아니라 `%LOCALAPPDATA%\DesktopWorklogToNotion\app\desktop-worklog-to-notion.exe`를 가리킨다.
- 시작프로그램 폴더:

```text
%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup
```

첫 실행 정책:

1. 다운로드한 exe를 실행한다.
2. exe가 자신을 `%LOCALAPPDATA%` 설치 위치로 복사한다.
3. 설정이 없으면 간단한 첫 실행 설정 흐름을 띄운다.
4. Notion token, Notion DB/data source ID를 입력받는다.
5. 기본 수집 폴더는 Desktop으로 둔다.
6. 시작프로그램 바로가리를 생성한다.
7. 설치 위치의 exe로 다음 실행부터 조용히 업로드하고 종료한다.

주의:

- 30분 내 성공 실행이 있으면 건너뛰기
- 동시에 두 프로세스가 돌지 않도록 lock 파일 사용
- 원본 exe가 삭제되어도 자동 실행은 유지되어야 한다.
- 설정은 `.env`가 아니라 `%APPDATA%\DesktopWorklogToNotion\settings.json`에 저장한다.
- `.env`는 오픈소스 개발자용 실행에서만 유지한다.

완료 기준:

- PyInstaller로 단일 exe 또는 배포 폴더가 생성된다.
- 첫 실행 후 `%LOCALAPPDATA%` 설치 위치에 exe가 복사된다.
- 시작프로그램 바로가리가 설치 위치 exe를 가리킨다.
- 원본 exe를 삭제해도 다음 로그인 때 실행된다.
- Python이 설치되지 않은 Windows 11 환경에서 실행된다.

## 3단계: desktop-worklog-to-notion 검증

### 테스트 시나리오

- 첫 실행 baseline 생성
- 두 번째 실행에서 새 파일 감지
- 기존 파일 수정 감지
- 파일 삭제 감지
- Git commit 감지
- Git 미커밋 변경 감지
- Excel 파일 분석
- CSV 인코딩 fallback
- Notion upsert 중복 방지
- Notion 토큰 오류
- Desktop 경로에 한글 포함

### 품질 기준

- 전체 실행이 파일 하나 때문에 실패하지 않는다.
- 실패한 항목은 로그에 남긴다.
- Notion에는 원문이 올라가지 않는다.
- README만 보고 설치할 수 있다.
- 기본 설정이 특정 사용자 경로를 포함하지 않는다.

## 4단계: browser-worklog-to-notion MVP

### 목표

브라우저에서 사용자가 작성한 텍스트를 Local Detail Mode로 로컬에 임시 저장하고, 브라우저 시작 또는 일일 알람 시 Notion에 작업 요약을 업로드한다.

### 권장 기술 스택

```text
TypeScript
Vite
Chrome Manifest V3
chrome.storage.local
chrome.alarms
규칙 기반 경량 요약
```

첫 버전에서는 무거운 NLP 라이브러리를 기본 번들에 넣지 않는다.

### 권장 저장소 구조

```text
browser-worklog-to-notion
├── README.md
├── package.json
├── tsconfig.json
├── vite.config.ts
├── manifest.json
├── public
│   └── icons
├── src
│   ├── background.ts
│   ├── contentScript.ts
│   ├── activityStore.ts
│   ├── captureEngine.ts
│   ├── summarizer.ts
│   ├── notionClient.ts
│   ├── domainPresets.ts
│   ├── sensitiveFilter.ts
│   ├── options.ts
│   └── popup.ts
├── options.html
├── popup.html
└── tests
    ├── summarizer.test.ts
    ├── sensitiveFilter.test.ts
    └── chunkDiff.test.ts
```

### 핵심 설계

Local Detail Mode:

- 사용자가 작성한 텍스트 chunk를 로컬에 임시 저장한다.
- Notion에는 raw text를 업로드하지 않는다.
- 업로드 직전에 로컬에서 요약한다.
- 업로드 성공 후 raw chunk를 삭제한다.

성능 원칙:

- 허용 도메인에서만 content script 실행
- focus된 입력 영역만 감시
- keydown 단위 저장 금지
- 5초 debounce
- snapshot 크기 제한
- chunk 크기 제한
- 하루 총 저장량 제한
- 요약 엔진 lazy load

### Manifest 권한

초기 권한:

```json
{
  "permissions": ["tabs", "storage", "alarms", "idle"],
  "host_permissions": [
    "https://api.notion.com/*",
    "https://github.com/*",
    "https://gitlab.com/*",
    "https://*.atlassian.net/*",
    "https://*.notion.so/*",
    "https://notion.so/*",
    "https://docs.google.com/*",
    "https://chatgpt.com/*",
    "https://chat.openai.com/*",
    "https://claude.ai/*",
    "https://gemini.google.com/*",
    "https://perplexity.ai/*"
  ]
}
```

주의:

- `<all_urls>`는 MVP 기본값에서 제외한다.
- 사용자가 도메인을 추가할 수 있게 한다.
- 민감 사이트 blocklist를 둔다.

### 구현 순서

#### 4.1 Vite + MV3 뼈대

구현:

- manifest 생성
- background service worker 등록
- content script 주입
- options page 생성
- popup page 생성

완료 기준:

- Chrome에서 unpacked extension으로 로드된다.
- options에서 Notion token/database id를 저장할 수 있다.

#### 4.2 도메인 프리셋

구현:

- developer preset
- ai tools preset
- user allowed domains
- blocked domains

기본 도메인:

```text
github.com
gitlab.com
bitbucket.org
dev.azure.com
atlassian.net
linear.app
notion.so
notion.site
docs.google.com
stackoverflow.com
chatgpt.com
chat.openai.com
claude.ai
gemini.google.com
perplexity.ai
platform.openai.com
```

완료 기준:

- 허용 도메인에서만 capture engine이 동작한다.
- blocked domain에서는 어떤 입력도 저장하지 않는다.

#### 4.3 작성 감지 엔진

감지 대상:

- `textarea`
- 일반 텍스트 `input`
- `contenteditable`
- `[role="textbox"]`
- `aria-multiline="true"`

제외:

- `input[type=password]`
- `input[type=hidden]`
- 카드/결제/OTP/autocomplete 민감 필드
- 20자 미만 짧은 입력

처리:

- focus된 element만 관찰
- `input`, `beforeinput`, `compositionend` 이벤트 처리
- 한글 IME 조합 중간 상태는 저장하지 않음
- 5초 debounce 후 snapshot 생성
- 이전 snapshot과 비교해 새 chunk 추출
- hash로 중복 제거

완료 기준:

- 일반 textarea에서 작성 chunk가 저장된다.
- contenteditable에서 작성 chunk가 저장된다.
- 비밀번호 필드는 저장되지 않는다.
- 한글 입력이 깨지지 않는다.

#### 4.4 민감정보 필터

필터:

- 이메일
- 전화번호
- 카드번호처럼 보이는 긴 숫자
- API key/token/private key 패턴
- `password`, `secret`, `token`, `api_key` 주변 텍스트

정책:

- 민감 패턴이 강하게 감지되면 chunk 전체 폐기
- 약한 패턴은 마스킹
- 폐기된 chunk 개수만 통계로 남김

완료 기준:

- 테스트 패턴이 저장되지 않는다.
- 민감정보가 Notion payload에 들어가지 않는다.

#### 4.5 로컬 저장소

저장 위치:

```text
chrome.storage.local
```

chunk 예시:

```json
{
  "id": "chunk_20260823_001",
  "created_at": "2026-08-23T15:20:00+09:00",
  "domain": "chatgpt.com",
  "page_title": "ChatGPT",
  "url_hint": "chatgpt.com",
  "field_type": "contenteditable",
  "text": "브라우저 확장 프로그램을 Local Detail Mode로 가볍게 구현하는 방식을 정리해줘.",
  "char_count": 45,
  "hash": "abc123",
  "uploaded": false
}
```

제한:

- `max_chunk_chars = 2000`
- `max_chunks_per_page_per_day = 50`
- `max_total_chars_per_day = 200000`
- `raw_retention_hours = 24`

완료 기준:

- 제한을 넘는 chunk는 잘리거나 저장되지 않는다.
- 오래된 raw chunk가 삭제된다.

#### 4.6 경량 요약 엔진

MVP는 규칙 기반으로 구현한다.

입력:

- chunk text
- page title
- domain
- created_at

처리:

- 문장 분리
- 불용어 제거
- 페이지 제목과 작성 문장 기반 주제 추정
- 중복 문장 제거
- 페이지별/도메인별 그룹화
- 작업 문장 템플릿 생성

출력 예시:

```text
- ChatGPT에서 브라우저 확장 프로그램의 Local Detail Mode 구현 방식을 작성했다.
- Notion에서 자동 작업일지 구현 계획을 정리했다.
- GitHub에서 오픈소스 배포 방향과 설정 구조를 작성했다.
```

주의:

- 요약 엔진은 content script에 넣지 않는다.
- 업로드 직전에 background에서 lazy load한다.
- 무거운 NLP 라이브러리는 optional로 둔다.

완료 기준:

- raw chunk 여러 개를 작업 문장 3~10개로 줄일 수 있다.
- Notion payload에 raw text가 들어가지 않는다.

#### 4.7 업로드 상태 관리

상태:

```json
{
  "last_success_at": "2026-08-21T23:30:00+09:00",
  "last_run_at": "2026-08-23T09:10:00+09:00",
  "pending_days": ["2026-08-22", "2026-08-23"]
}
```

트리거:

- `runtime.onStartup`
- `runtime.onInstalled`
- `chrome.alarms`
- 사용자가 popup에서 수동 업로드

정책:

- 브라우저 시작 시 `last_success_at` 이후 chunk가 있으면 업로드한다.
- 브라우저가 켜져 있으면 매일 23:30 업로드한다.
- 업로드 성공 후 raw chunk 삭제
- 실패 시 raw chunk 유지
- 다음 실행에서 재시도

완료 기준:

- 브라우저 재시작 후 미업로드 chunk가 전송된다.
- 실패한 업로드는 다음 실행에서 재시도된다.

#### 4.8 Notion 업로드

구현:

- Notion token/database id는 options에서 입력
- `Date + Source + Project` 기준 upsert
- `Source = Browser`
- `Written Chunks`, `Main Domains` 속성 채우기
- 본문에 브라우저 작성 요약과 근거 통계 추가

본문 예시:

```text
브라우저에서 작성한 것

- ChatGPT에서 브라우저 확장 프로그램의 Local Detail Mode 구현 방식을 작성했다.
- Notion에서 자동 작업일지 구현 계획을 정리했다.
- GitHub에서 오픈소스 배포 방향을 검토했다.

근거

- 작성 chunk: 24개
- 주요 사이트: chatgpt.com, notion.so, github.com
- 작성 근거: 페이지 제목, 작성 시각, 도메인, chunk 수
```

완료 기준:

- Notion에 Browser source 페이지가 생성된다.
- 중복 페이지가 생기지 않는다.
- raw text가 payload에 포함되지 않는다.

## 5단계: browser-worklog-to-notion 검증

### 테스트 시나리오

- textarea 작성 감지
- contenteditable 작성 감지
- 한글 입력 감지
- 입력 후 삭제한 텍스트 처리
- 짧은 입력 제외
- 비밀번호 필드 제외
- blocked domain 제외
- allowed domain에서만 동작
- raw chunk 저장 제한
- raw chunk 보관 기간 만료 삭제
- 요약 결과 생성
- Notion 업로드 성공 후 raw 삭제
- Notion 업로드 실패 후 raw 유지
- 브라우저 재시작 후 재업로드

### 실사용 검증 사이트

우선 검증:

```text
chatgpt.com
github.com
notion.so
docs.google.com
atlassian.net
stackoverflow.com
```

성공 기준:

- 6개 사이트 중 4개 이상에서 의미 있는 작성 chunk가 잡힌다.
- 민감 입력은 저장되지 않는다.
- 평소 브라우징 체감 성능 저하가 없다.
- 하루치 작업 요약이 읽을 만한 수준으로 나온다.

## 6단계: 배포 문서

### desktop README에 포함할 것

- 이 도구가 하는 일
- 이 도구가 하지 않는 일
- Notion token 만드는 법
- Database schema 만드는 법
- 설치 방법
- 첫 실행 정책
- Windows 11 exe 실행 방법
- 시작프로그램 자동 실행 방식
- AppData 설치 위치
- 개인정보 정책
- 문제 해결

### browser README에 포함할 것

- 이 확장 프로그램이 작성 텍스트를 로컬에 임시 저장한다는 고지
- Notion에는 원문을 업로드하지 않는다는 설명
- Local Detail Mode 설명
- 허용 도메인/차단 도메인 설정 방법
- Notion token 저장 위치
- raw chunk 삭제 정책
- 지원이 약할 수 있는 사이트
- Chrome unpacked 설치 방법
- Chrome Web Store 배포 시 권한 설명

### 라이선스

권장:

```text
MIT License
```

단, README에는 개인정보 관련 면책과 사용자 책임을 명확히 쓴다.

## 주요 리스크와 대응

### Desktop 프로젝트 리스크

리스크:

- Desktop 전체 스캔으로 노이즈가 많다.
- 파일명에도 민감정보가 있을 수 있다.
- 스캔 시간이 길어질 수 있다.

대응:

- 강한 제외 규칙
- 파일명 마스킹 옵션
- 최대 파일 크기 제한
- 원문 업로드 금지
- dry-run으로 사전 확인

### Browser 프로젝트 리스크

리스크:

- 작성 텍스트를 로컬에 저장하므로 민감하다.
- 모든 사이트에서 완벽히 동작하지 않는다.
- Chrome MV3 service worker는 항상 살아 있지 않다.
- Google Docs 같은 복잡한 에디터는 감지가 불안정할 수 있다.
- 요약 품질이 AI보다 약하다.

대응:

- 오픈소스 코드 공개
- Local Detail Mode 명확 고지
- raw text Notion 업로드 금지
- 업로드 성공 후 raw 삭제
- 허용 도메인 기반
- 민감정보 필터
- 이벤트 기반 설계
- 요약 엔진 lazy load
- fallback으로 page title/domain/작성량 기반 요약

## 자동 구현 시 우선순위

### 반드시 먼저 구현

1. Notion upsert
2. 상태 저장 `last_success_at`
3. Desktop 파일 스캔
4. Desktop Git 수집
5. Desktop dry-run
6. Browser chunk capture
7. Browser local storage
8. Browser raw 삭제 정책
9. Browser 요약 엔진

### 나중에 구현

- Chrome Web Store 배포
- 복잡한 사이트별 extractor
- 고급 NLP 라이브러리
- GUI 트레이 앱
- PDF/DOCX 분석
- Excel 셀 단위 diff
- 여러 Notion Database 자동 생성

### 구현하지 않을 것

- 키로거 방식의 keydown 저장
- 파일 원문 Notion 업로드
- 브라우저 raw text Notion 업로드
- 화면 캡처
- 웹페이지 본문 전체 수집
- 브라우저 쿠키/localStorage/IndexedDB 수집
- 서비스별 비공식 내부 API 호출

## 최종 완료 기준

두 프로젝트가 다음 기준을 만족하면 1차 완성으로 본다.

Desktop:

- Windows Desktop 변경분을 날짜별로 Notion에 기록한다.
- Git commit/status가 기록된다.
- Notion Calendar view에서 날짜별로 보인다.
- 첫 실행과 재실행이 안전하다.
- 원문이 업로드되지 않는다.

Browser:

- 허용 도메인에서 사용자가 작성한 chunk를 로컬에 저장한다.
- 브라우저 시작 시 이전 성공 업로드 이후 chunk를 전송한다.
- Notion에는 요약만 올라간다.
- 업로드 성공 후 raw chunk가 삭제된다.
- 메모리 사용을 낮게 유지한다.

공통:

- README만 보고 설치할 수 있다.
- 개인 설정과 예시 설정이 분리되어 있다.
- Notion 중복 페이지가 생기지 않는다.
- 실패해도 다음 실행에서 복구할 수 있다.

## 다음 단계 실행용 작업 분해

이 섹션은 사용자가 이후에 `다음 단계 해줘`라고만 말해도 순서대로 구현할 수 있도록 작업을 잘게 나눈다.

진행 규칙:

- 한 번에 하나의 단계만 구현한다.
- 각 단계는 작은 PR처럼 끝낸다.
- 각 단계마다 직접 실행 가능한 테스트 또는 검증 명령을 남긴다.
- 다음 단계로 넘어가기 전에 이전 단계의 완료 기준을 만족해야 한다.
- 실패한 테스트나 미완료 항목은 다음 단계로 넘기지 않는다.

## Desktop 프로젝트 세부 단계

### D-00. 저장소 초기화

목표:

- `desktop-worklog-to-notion` 폴더를 만들고 Python 프로젝트 기본 구조를 만든다.

구현 파일:

```text
desktop-worklog-to-notion/
├── README.md
├── requirements.txt
├── .gitignore
├── .env.example
├── worklog/__init__.py
└── worklog/cli.py
```

구현 내용:

- `requirements.txt`에 최소 패키지 추가
- `.gitignore`에 `.env`, `.worklog/state`, `.worklog/logs`, `__pycache__` 추가
- `python -m worklog.cli --help`가 동작하는 CLI 뼈대 작성

직접 테스트:

```powershell
cd desktop-worklog-to-notion
python -m venv .venv
.\.venv\Scripts\python -m pip install -r requirements.txt
.\.venv\Scripts\python -m worklog.cli --help
```

완료 기준:

- CLI help가 출력된다.
- import 오류가 없다.

### D-01. 설정 파일 초기화

목표:

- `worklog init` 명령으로 개인 `.env` 설정 파일을 생성한다.

구현 파일:

```text
worklog/config.py
worklog/cli.py
.env.example
```

구현 내용:

- `.env.example` 작성
- `worklog init` 구현
- 이미 `.env`가 있으면 덮어쓰지 않음
- `%USERPROFILE%/Desktop` 기본값 사용

직접 테스트:

```powershell
.\.venv\Scripts\python -m worklog.cli init
Test-Path .env
Get-Content .env
.\.venv\Scripts\python -m worklog.cli init
```

완료 기준:

- 첫 실행 때 `.env`가 생성된다.
- 두 번째 실행 때 기존 설정을 보존한다.

### D-02. 경로 확장 유틸

목표:

- Windows 사용자별 Desktop 경로를 안전하게 해석한다.

구현 파일:

```text
worklog/paths.py
tests/test_paths.py
```

구현 내용:

- `%USERPROFILE%`, `${USERPROFILE}`, `~` 확장
- `/`, `\` 모두 허용
- 존재하지 않는 경로는 오류가 아니라 warning 대상

직접 테스트:

```powershell
.\.venv\Scripts\python -m pytest tests/test_paths.py
.\.venv\Scripts\python -m worklog.cli dry-run --no-upload
```

완료 기준:

- 테스트 통과
- 현재 사용자 Desktop 경로가 올바르게 출력된다.

### D-03. 상태 저장

목표:

- `run_state.json`을 읽고 쓰며 `last_success_at` 정책을 구현한다.

구현 파일:

```text
worklog/state.py
tests/test_state.py
```

구현 내용:

- state 파일이 없으면 첫 실행 상태 반환
- `last_success_at`, `last_run_at`, `timezone` 저장
- 성공 시에만 `last_success_at` 갱신하는 함수 작성

직접 테스트:

```powershell
.\.venv\Scripts\python -m pytest tests/test_state.py
.\.venv\Scripts\python -m worklog.cli status
```

완료 기준:

- 첫 실행 상태를 감지한다.
- 상태 저장 후 다시 읽을 수 있다.

### D-04. 파일 스캐너

목표:

- Desktop 하위 파일을 제외 규칙에 맞게 스캔한다.

구현 파일:

```text
worklog/scanner.py
tests/test_scanner.py
```

구현 내용:

- root_paths 순회
- 제외 폴더/확장자/name pattern 적용
- 파일 크기 제한 적용
- mtime, size, extension, relative path 수집
- 권한 오류는 skipped 목록에 기록

직접 테스트:

```powershell
New-Item -ItemType Directory -Force test_workspace\DesktopLike | Out-Null
Set-Content test_workspace\DesktopLike\a.md "hello"
Set-Content test_workspace\DesktopLike\b.log "skip"
.\.venv\Scripts\python -m pytest tests/test_scanner.py
.\.venv\Scripts\python -m worklog.cli dry-run --root .\test_workspace\DesktopLike --no-upload
```

완료 기준:

- `.md`는 수집되고 `.log`는 제외된다.
- 스캔 결과가 콘솔 또는 JSON으로 확인된다.

### D-05. 해시와 스냅샷

목표:

- 파일 해시를 계산하고 이전 스냅샷과 비교한다.

구현 파일:

```text
worklog/snapshot.py
tests/test_snapshot.py
```

구현 내용:

- SHA-256 계산
- `latest_snapshot.json` 저장
- 날짜별 스냅샷 저장
- 새 파일/수정 파일/삭제 파일 분류

직접 테스트:

```powershell
.\.venv\Scripts\python -m pytest tests/test_snapshot.py
.\.venv\Scripts\python -m worklog.cli dry-run --root .\test_workspace\DesktopLike --no-upload
Add-Content test_workspace\DesktopLike\a.md "changed"
.\.venv\Scripts\python -m worklog.cli dry-run --root .\test_workspace\DesktopLike --no-upload
```

완료 기준:

- 두 번째 실행에서 수정 파일이 감지된다.

### D-06. 텍스트/CSV/Excel 분석

목표:

- 파일 타입별 구조 정보만 추출한다.

구현 파일:

```text
worklog/analyzers.py
tests/test_analyzers.py
```

구현 내용:

- 텍스트 라인 수/글자 수/제목 후보
- CSV 컬럼/행/열 수
- Excel 시트명/행/열 수/헤더 후보
- 원문 업로드용 데이터는 만들지 않음

직접 테스트:

```powershell
.\.venv\Scripts\python -m pytest tests/test_analyzers.py
.\.venv\Scripts\python -m worklog.cli dry-run --root .\test_workspace\DesktopLike --no-upload
```

완료 기준:

- 분석 실패 파일이 있어도 전체 실행은 계속된다.
- 분석 결과에 원문 본문이 포함되지 않는다.

### D-07. Git 수집

목표:

- Desktop 하위 Git repo를 찾아 Git 정보를 수집한다.

구현 파일:

```text
worklog/git_collector.py
tests/test_git_collector.py
```

구현 내용:

- `.git` 폴더 탐색
- branch, 오늘/기간 내 commit, status, diff stat 수집
- diff 원문은 수집하지 않음

직접 테스트:

```powershell
New-Item -ItemType Directory -Force test_workspace\repo | Out-Null
cd test_workspace\repo
git init
git config user.email test@example.com
git config user.name tester
Set-Content readme.md "hello"
git add .
git commit -m "initial worklog test"
cd ..\..
.\.venv\Scripts\python -m pytest tests/test_git_collector.py
.\.venv\Scripts\python -m worklog.cli dry-run --root .\test_workspace --no-upload
```

완료 기준:

- 테스트 repo의 commit 메시지가 수집된다.
- Git이 없는 폴더에서도 오류가 나지 않는다.

### D-08. Desktop 요약 생성

목표:

- 수집 결과를 Notion에 올릴 작업 문장으로 만든다.

구현 파일:

```text
worklog/summarizer.py
tests/test_summarizer.py
```

구현 내용:

- 파일 타입별 요약
- 폴더별 변경량 요약
- Git commit 기반 요약
- 미커밋 변경사항 요약
- 변경이 없을 때 처리

직접 테스트:

```powershell
.\.venv\Scripts\python -m pytest tests/test_summarizer.py
.\.venv\Scripts\python -m worklog.cli dry-run --root .\test_workspace --no-upload
```

완료 기준:

- 사람이 읽을 수 있는 bullet summary가 생성된다.
- 원문이 포함되지 않는다.

### D-09. Notion client dry-run payload

목표:

- 실제 업로드 전에 Desktop Notion payload를 JSON으로 검증한다.
- Browser 프로젝트와 같은 공통 스키마를 사용한다.

구현 파일:

```text
worklog/notion_client.py
tests/test_notion_payload.py
```

구현 내용:

- Notion properties 생성
- Notion body blocks 생성
- raw content가 payload에 없는지 검사
- `Keywords` 속성을 생성하거나 전송하지 않음
- 기존 title 속성이 `Name`이 아니라 `이름`이어도 사용할 수 있도록 설계
- `database_id`와 `data_source_id`를 모두 설정값으로 받을 수 있도록 입력 구조 정리

직접 테스트:

```powershell
.\.venv\Scripts\python -m pytest tests/test_notion_payload.py
.\.venv\Scripts\python -m worklog.cli dry-run --root .\test_workspace --output .\test_workspace\out.json --no-upload
Get-Content .\test_workspace\out.json
```

완료 기준:

- payload JSON이 생성된다.
- raw file content가 포함되지 않는다.
- `Keywords` property가 payload에 없다.
- Browser Worklog와 같은 Database에 넣어도 충돌하지 않는다.

### D-10. Notion 실제 업로드

목표:

- Notion Database에 Desktop 작업일지를 upsert한다.
- 최신 Notion `data_sources` API와 기존 `databases` API 차이를 흡수한다.

구현 파일:

```text
worklog/notion_client.py
worklog/cli.py
```

구현 내용:

- `NOTION_TOKEN` 읽기
- 설정값이 database ID인지 data source ID인지 판별
- data source 조회
- database 조회 fallback
- 접근 가능한 data source 검색 또는 검증
- data source query
- page create
- page update
- block append
- 429 retry
- 404 `object_not_found` 발생 시 권한 문제와 ID 문제를 구분하는 에러 메시지
- `Cannot create new title property`가 나지 않도록 기존 title 속성 재사용
- schema ensure 시 기존 호환 속성은 유지하고 누락 속성만 추가

직접 테스트:

```powershell
$env:NOTION_TOKEN="ntn_xxx"
.\.venv\Scripts\python -m worklog.cli run --root .\test_workspace
.\.venv\Scripts\python -m worklog.cli run --root .\test_workspace
```

수동 확인:

- Notion Database에 페이지가 생겼는지 확인
- 두 번째 실행에서 중복 페이지가 생기지 않는지 확인
- Calendar view에서 Date 기준으로 보이는지 확인
- `Source = Desktop`으로 필터링되는지 확인
- 기존 Browser Worklog 페이지와 같은 DB에서 충돌하지 않는지 확인

완료 기준:

- Notion page 생성 성공
- upsert 성공
- raw content 미포함
- 최신 Notion 권한 방식에서 `data_source_id`로 업로드 성공
- 기존 database ID를 넣어도 가능한 범위에서 자동으로 data source를 찾아 업로드 성공

### EXE-01. Windows 11 exe 빌드

목표:

- Python이 없는 Windows 11 사용자도 실행할 수 있도록 PyInstaller 빌드를 추가한다.

구현 파일:

```text
desktop-worklog-to-notion.spec
scripts/build-exe.ps1
README.md
```

구현 내용:

- PyInstaller 의존성 추가
- exe 빌드 스크립트 작성
- dist 배포 폴더 생성
- exe에서 `worklog.cli`가 정상 실행되는지 확인
- Windows 11 전용으로 문서화

직접 테스트:

```powershell
.\scripts\build-exe.ps1
.\dist\desktop-worklog-to-notion\desktop-worklog-to-notion.exe --help
.\dist\desktop-worklog-to-notion\desktop-worklog-to-notion.exe dry-run
```

완료 기준:

- Windows 11에서 Python 없이 exe가 실행된다.
- README에 exe 실행 방식이 정리된다.

### EXE-02. AppData 설치 복사

목표:

- 다운로드한 원본 exe 위치에 의존하지 않도록 안정적인 설치 위치를 만든다.

구현 파일:

```text
worklog/installer.py
worklog/cli.py
tests/test_installer.py
```

구현 내용:

- `%LOCALAPPDATA%\DesktopWorklogToNotion\app` 생성
- 실행 중인 exe를 설치 위치로 복사
- 설치 위치에서 실행 중인지 감지
- 원본 exe에서 실행하면 설치 위치 exe를 실행하고 원본 프로세스는 종료

완료 기준:

- 원본 exe를 삭제해도 설치 위치 exe가 남는다.

### EXE-03. 첫 실행 설정 저장

목표:

- 비개발자가 `.env`를 직접 수정하지 않아도 되게 한다.

구현 파일:

```text
worklog/user_settings.py
worklog/cli.py
tests/test_user_settings.py
```

구현 내용:

- `%APPDATA%\DesktopWorklogToNotion\settings.json` 저장
- Notion token, database/data source ID 저장
- 기본 root path는 Desktop
- 개발자용 `.env` fallback은 유지

완료 기준:

- settings.json만 있어도 run이 가능하다.
- `.env`는 없어도 exe 실행이 가능하다.

### EXE-04. 시작프로그램 바로가기 등록

목표:

- PC 로그인 시 설치 위치 exe가 자동 실행되게 한다.

구현 파일:

```text
worklog/startup.py
tests/test_startup.py
```

구현 내용:

- 시작프로그램 폴더 경로 계산
- 설치 위치 exe를 가리키는 `.lnk` 생성
- 기존 잘못된 바로가리가 있으면 교체
- 자동 실행 끄기 기능은 바로가기 삭제로 처리

완료 기준:

- 바로가리가 `%LOCALAPPDATA%` 설치 위치 exe를 가리킨다.
- 원본 다운로드 exe를 삭제해도 자동 실행 대상이 깨지지 않는다.

## Browser 프로젝트 세부 단계

### B-00. 저장소 초기화

목표:

- `browser-worklog-to-notion` 확장 프로그램 프로젝트를 만든다.

구현 파일:

```text
browser-worklog-to-notion/
├── README.md
├── package.json
├── tsconfig.json
├── vite.config.ts
├── manifest.json
├── src/background.ts
├── src/contentScript.ts
├── src/options.ts
├── src/popup.ts
├── options.html
└── popup.html
```

직접 테스트:

```powershell
cd browser-worklog-to-notion
npm install
npm run build
```

수동 확인:

- Chrome `chrome://extensions`에서 Developer mode 켜기
- `dist` 폴더를 Load unpacked
- 확장 프로그램이 로드되는지 확인

완료 기준:

- 빌드 성공
- Chrome에 unpacked extension 로드 성공

### B-01. Options 저장

목표:

- Notion token, database id, 설정값을 저장한다.

구현 파일:

```text
src/options.ts
options.html
src/settingsStore.ts
```

구현 내용:

- Notion token 입력
- Database ID 입력
- capture enabled toggle
- raw retention 설정
- allowed domain 목록 표시
- `chrome.storage.local` 저장

직접 테스트:

```powershell
npm test
npm run build
```

수동 확인:

- options 페이지 열기
- 값 입력 후 저장
- options 페이지를 닫고 다시 열어 값이 유지되는지 확인

완료 기준:

- 설정 저장/로드 성공

### B-02. 도메인 프리셋

목표:

- 개발자용 도메인과 AI 사이트 프리셋을 코드화한다.

구현 파일:

```text
src/domainPresets.ts
src/domainMatcher.ts
tests/domainMatcher.test.ts
```

구현 내용:

- developer preset
- ai tools preset
- blocked domains
- user allowed domains
- subdomain matching

직접 테스트:

```powershell
npm test -- domainMatcher
```

완료 기준:

- `chatgpt.com`, `github.com`, `notion.so` 허용
- `gmail.com`, `accounts.google.com`, `stripe.com` 차단

### B-03. Content script 주입 조건

목표:

- 허용 도메인에서만 capture engine이 동작하게 한다.

구현 파일:

```text
src/contentScript.ts
src/domainMatcher.ts
```

직접 테스트:

```powershell
npm run build
```

수동 확인:

- 허용 도메인 테스트 페이지에서 console debug가 보인다.
- 차단 도메인에서는 아무 저장 이벤트가 발생하지 않는다.

완료 기준:

- 허용/차단 도메인 정책이 실제 탭에서 동작한다.

### B-04. 기본 작성 감지

목표:

- textarea/input 작성 chunk를 저장한다.

구현 파일:

```text
src/captureEngine.ts
src/contentScript.ts
tests/chunkDiff.test.ts
```

구현 내용:

- focus된 input/textarea 감지
- 5초 debounce
- snapshot 비교
- chunk 생성
- 20자 미만 제외

직접 테스트:

```powershell
npm test -- chunkDiff
npm run build
```

수동 확인용 HTML:

```html
<textarea id="t"></textarea>
<input id="i" type="text" />
<input id="p" type="password" />
```

수동 확인:

- textarea에 긴 문장 입력
- 5초 후 chunk 저장 확인
- password 입력은 저장되지 않는지 확인

완료 기준:

- 일반 입력 chunk 저장
- password 제외

### B-05. contenteditable 감지

목표:

- Notion류 에디터에서 쓰는 contenteditable 작성 이벤트를 감지한다.

구현 파일:

```text
src/captureEngine.ts
tests/captureEngine.test.ts
```

구현 내용:

- `[contenteditable=true]`
- `[role=textbox]`
- `aria-multiline=true`
- `compositionstart`, `compositionend` 처리
- 한글 조합 중간 저장 방지

직접 테스트:

```powershell
npm test -- captureEngine
npm run build
```

수동 확인:

- contenteditable 테스트 페이지에서 한글 문장 입력
- 조합 중간 텍스트가 저장되지 않는지 확인
- 최종 문장 chunk만 저장되는지 확인

완료 기준:

- contenteditable 저장 성공
- 한글 입력 깨짐 없음

### B-06. 민감정보 필터

목표:

- 저장 전 민감정보를 폐기하거나 마스킹한다.

구현 파일:

```text
src/sensitiveFilter.ts
tests/sensitiveFilter.test.ts
```

구현 내용:

- email
- phone
- card-like number
- api key
- token
- private key
- password/secret 주변 텍스트

직접 테스트:

```powershell
npm test -- sensitiveFilter
```

완료 기준:

- 민감 chunk는 저장되지 않는다.
- 약한 민감값은 마스킹된다.

### B-07. Activity store

목표:

- chunk를 `chrome.storage.local`에 저장하고 제한을 적용한다.

구현 파일:

```text
src/activityStore.ts
tests/activityStore.test.ts
```

구현 내용:

- chunk 저장
- hash 중복 제거
- 날짜별 조회
- `last_success_at` 이후 조회
- max chunk/page/day 제한
- max total chars/day 제한
- raw retention 만료 삭제

직접 테스트:

```powershell
npm test -- activityStore
npm run build
```

수동 확인:

- 여러 chunk 입력 후 storage 확인
- 제한 초과 시 저장이 제한되는지 확인

완료 기준:

- 저장 제한 동작
- 오래된 raw 삭제 동작

### B-08. Popup 상태 화면

목표:

- 현재 수집 상태를 사용자가 확인할 수 있게 한다.

구현 파일:

```text
popup.html
src/popup.ts
```

표시:

- 오늘 저장된 chunk 수
- 마지막 업로드 시각
- pending days
- capture enabled 상태
- 수동 업로드 버튼

직접 테스트:

```powershell
npm run build
```

수동 확인:

- popup을 열어 chunk count 확인
- capture on/off 전환

완료 기준:

- 사용자가 현재 상태를 알 수 있다.

### B-09. 경량 요약 엔진

목표:

- raw chunk를 Notion에 올릴 작업 문장으로 변환한다.

구현 파일:

```text
src/summarizer.ts
tests/summarizer.test.ts
```

구현 내용:

- 문장 분리
- 페이지 제목 가중치
- 작성 문장 기반 주제 추정
- 도메인별 그룹화
- 중복 문장 제거
- 3~10개 작업 문장 생성

직접 테스트:

```powershell
npm test -- summarizer
```

완료 기준:

- raw chunk가 summary bullets로 변환된다.
- raw text 전체가 summary에 그대로 복사되지 않는다.

### B-10. Notion payload 생성

목표:

- Browser source용 Notion payload를 만든다.

구현 파일:

```text
src/notionPayload.ts
tests/notionPayload.test.ts
```

구현 내용:

- properties 생성
- body blocks 생성
- `Source = Browser`
- `Written Chunks`, `Main Domains`
- 작성 내용과 근거 본문 생성
- raw text 미포함 검사

직접 테스트:

```powershell
npm test -- notionPayload
```

완료 기준:

- payload에 raw chunk text가 없다.

### B-11. Notion 실제 업로드

목표:

- Notion API로 Browser Worklog를 upsert한다.

구현 파일:

```text
src/notionClient.ts
src/background.ts
```

구현 내용:

- database query
- create page
- update page
- append blocks
- 429 retry
- 실패 시 raw 유지

직접 테스트:

```powershell
npm run build
```

수동 확인:

- options에 Notion token/database id 입력
- 테스트 chunk 생성
- popup에서 수동 업로드
- Notion 페이지 생성 확인
- 다시 업로드해 중복 페이지가 생기지 않는지 확인

완료 기준:

- Browser source 페이지 생성
- upsert 성공
- raw text 미업로드

### B-12. 브라우저 시작 시 보충 업로드

목표:

- 브라우저를 켤 때 이전 성공 이후 chunk를 업로드한다.

구현 파일:

```text
src/background.ts
src/activityStore.ts
```

구현 내용:

- `runtime.onStartup`
- `runtime.onInstalled`
- `chrome.alarms`
- `last_success_at` 이후 조회
- 업로드 성공 후 `last_success_at` 갱신

직접 테스트:

```powershell
npm run build
```

수동 확인:

- chunk 저장
- 브라우저 종료
- 브라우저 재시작
- 업로드 시도 로그 확인
- Notion 페이지 확인

완료 기준:

- 재시작 후 pending chunk가 업로드된다.

### B-13. 메모리/성능 확인

목표:

- 확장 프로그램이 브라우저를 무겁게 만들지 않는지 확인한다.

검증 방법:

- Chrome Task Manager 열기
- 확장 프로그램 메모리 사용량 확인
- 허용 도메인 탭 5개 열기
- 긴 textarea 입력 테스트
- contenteditable 입력 테스트

수동 체크:

```text
1. Chrome Task Manager에서 extension process 확인
2. 입력 전 메모리 기록
3. 긴 글 5분 작성
4. 입력 후 메모리 기록
5. 탭 전환 후 메모리 증가가 계속 누적되는지 확인
6. raw chunk가 storage로 이동하고 메모리에 남지 않는지 확인
```

완료 기준:

- 일반 사용 중 체감 지연이 없다.
- 메모리가 계속 증가하지 않는다.
- content script가 비허용 도메인에서 동작하지 않는다.

## 내가 직접 확인할 공통 검증 항목

Desktop 구현 중:

- `pytest` 전체 실행
- `dry-run` JSON 확인
- 한글 경로 테스트
- Git repo 없는 폴더 테스트
- Git repo 있는 폴더 테스트
- Notion payload raw content 미포함 검사

Browser 구현 중:

- `npm test` 전체 실행
- `npm run build`
- Chrome unpacked 로드
- 허용 도메인/차단 도메인 수동 확인
- textarea/contenteditable/password 수동 확인
- 한글 입력 수동 확인
- popup 상태 확인
- Notion 수동 업로드 확인
- 브라우저 재시작 후 보충 업로드 확인
- Chrome Task Manager로 메모리 확인

Notion 확인:

- Calendar view에서 날짜별 표시
- `Source = Desktop` 필터
- `Source = Browser` 필터
- 중복 페이지 없음
- 실패 후 재시도 가능

## 다음 단계 명령 기준

이후 사용자가 `다음 단계 해줘`라고 하면 현재 진행 상태 기준으로 다음 순서로 진행한다.

이미 완료되었거나 상당 부분 구현된 단계:

```text
D-00 ~ D-08
D-09R
D-10R
ENV-01
B-00 ~ B-11
README 1차 분리
```

앞으로 진행할 순서:

1. D-09R. Desktop Notion payload 재검증 [완료]
   - Browser와 같은 공통 스키마 사용
   - `Keywords` 제거 확인
   - title 속성 `Name`/`이름` 호환 확인
   - raw file content 미포함 확인

2. D-10R. Desktop Notion client 최신화 [완료]
   - 최신 Notion `data_sources` API 대응
   - 기존 `databases` API fallback
   - database ID와 data source ID 모두 수용
   - 권한 오류 메시지 개선
   - schema ensure 안정화

3. D-10P. Desktop dry-run 성능/진행 표시 보완 - 완료
   - Desktop 전체 스캔이 오래 걸릴 때 진행 상태 출력
   - 스캔 파일 수/분석 파일 수 제한 또는 경고 추가
   - `dry-run`이 업로드 성공 전 스냅샷 상태를 저장하지 않는지 확인
   - 실제 Desktop 기준으로 30초 이상 무응답이 생기지 않게 조정
   - 검증 결과: Desktop 전체 기준 `dry-run`은 약 6초 내 완료
   - 기본 상한: 스캔 5000개, 분석 500개, 파일 내용 해시 비활성화
   - 검증 결과: `dry-run`은 `.worklog/state`에 스냅샷을 저장하지 않음
   - 첫 실행은 기준 스냅샷만 만들고 Notion 업로드는 건너뛰도록 보정
   - Git commit은 마지막 기준 시점 이후이며 author가 현재 사용자와 맞는 commit만 집계
   - `.worklog`, Gradle 캐시, 바이너리/잠금 파일 등 작업일지에 부적절한 잡음 제외
   - 깨진 한글 요약 문구와 Notion 본문 라벨 정상화
   - 검증 결과: `pytest` 71개 통과

4. D-10T. Desktop 실제 업로드 테스트 - 완료
   - `dry-run`으로 Desktop 수집 결과 확인
   - 테스트 Notion DB에 `Source = Desktop` 페이지 생성 확인
   - 두 번째 실행에서 중복 페이지가 생기지 않는지 확인
   - 수동 재실행 검증을 위해 `run --force` 옵션 추가
   - 검증 결과: Notion page `3c551ebe-b8a9-8144-bc03-d4c1968a53b2` 갱신 성공
   - 검증 결과: 두 번째 실행도 같은 page ID를 갱신하여 중복 생성 없음
   - 검증 결과: 두 번째 실행에서 파일 변경 0개, Git commit 0개로 기준 스냅샷 갱신 확인

5. EXE-01. Windows 11 exe 빌드 - 완료
   - PyInstaller 설정 추가
   - exe 빌드 스크립트 추가
   - Python 없는 사용자를 위한 실행 방식 문서화
   - `desktop_worklog_to_notion.py` PyInstaller 진입점 추가
   - `requirements-dev.txt`, `scripts/build-exe.ps1` 추가
   - 검증 결과: `dist\desktop-worklog-to-notion.exe` 생성 성공
   - 검증 결과: exe `--help`, `status` 실행 성공
   - 확인 사항: 현재 exe는 개발용 CLI 패키징이며 AppData 설치/설정 저장은 EXE-02/EXE-03에서 구현

6. EXE-02. AppData 설치 복사 - 완료
   - 원본 다운로드 폴더에 의존하지 않게 설치 위치로 자기 복사
   - `%LOCALAPPDATA%` 설치 위치 기준 실행
   - frozen exe 실행 시 `%LOCALAPPDATA%\DesktopWorklogToNotion\app\desktop-worklog-to-notion.exe`로 자기 복사
   - frozen exe 상태 저장 위치를 `%LOCALAPPDATA%\DesktopWorklogToNotion\state`로 분리
   - 검증 결과: dist exe 실행 시 설치 위치 exe 생성 성공
   - 검증 결과: 설치 위치 exe `status` 실행 성공

7. EXE-03. 첫 실행 설정 저장 - 완료
   - 비개발자용 `settings.json` 저장
   - `.env` 없이 exe 실행 가능하게 구성
   - `%APPDATA%\DesktopWorklogToNotion\settings.json` 저장/로드 추가
   - `setup` 명령 추가
   - frozen exe를 인자 없이 실행하면 설정 없음=`setup`, 설정 있음=`run`으로 동작
   - 검증 결과: 개발 실행 `setup` 저장 성공
   - 검증 결과: exe 인자 없는 첫 실행에서 AppData 복사와 settings.json 생성 성공
   - 검증 결과: `pytest` 78개 통과

8. EXE-04. 시작프로그램 바로가기 등록 - 완료
   - 작업 스케줄러 대신 시작프로그램 폴더 사용
   - 바로가리는 설치 위치 exe를 가리키게 생성
   - `setup` 성공 후 `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\Desktop Worklog to Notion.lnk` 생성
   - `install-startup` 명령 추가
   - 검증 결과: exe 첫 실행에서 settings.json, 설치 위치 exe, Startup `.lnk` 생성 성공
   - 검증 결과: `pytest` 81개 통과

9. EXE-05. 제거 exe 제공 - 완료
   - `uninstall.exe` 빌드 추가
   - 메인 exe `uninstall` 명령 추가
   - `uninstall --keep-settings` 지원
   - 제거 대상: Startup `.lnk`, `%LOCALAPPDATA%\DesktopWorklogToNotion`, `%APPDATA%\DesktopWorklogToNotion\settings.json`
   - 검증 결과: 임시 AppData 환경에서 설치/설정/바로가기 생성 후 uninstall exe로 모두 제거 성공
   - 검증 결과: `pytest` 83개 통과

10. B-12R. Browser 시작 시 보충 업로드 검증
   - 브라우저 재시작 후 미업로드 chunk 전송 확인
   - 실패한 업로드가 다음 실행에서 재시도되는지 확인

10. B-13R. Browser 메모리/성능 확인
   - 허용 도메인에서만 content script 동작 확인
   - popup/background가 과하게 유지되지 않는지 확인
   - Chrome Task Manager로 메모리 확인

11. I-01. 두 프로젝트 통합 검증
   - 같은 Notion DB에 Desktop/Browser 페이지가 함께 쌓이는지 확인
   - Calendar view에서 둘 다 보이는지 확인
   - Source 필터가 제대로 동작하는지 확인

12. DOC-02. 공개용 문서 마감
   - 루트 README는 입구 역할만 유지
   - 브라우저용 README는 설치 성공 경로만 유지
   - PC용 README는 설치 성공 경로만 유지
   - 자세한 오류는 `문제해결.md`로 분리 유지

각 단계 완료 후에는 다음을 보고한다.

```text
완료한 단계:
수정/생성한 파일:
실행한 테스트:
테스트 결과:
남은 리스크:
다음 단계:
```
